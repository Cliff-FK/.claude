---
paths:
  - "**/*.astro"
  - "**/astro.config.*"
---

# Invariants Astro (natif, perf-first)

- **Avant toute feature Astro : invoquer le skill `cliff-stack:astro-native`** (nom qualifié obligatoire, le nom court échoue) (vérif API via Context7, zero-JS par défaut, LCP comme garde-fou dur).
