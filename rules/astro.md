---
paths:
  - "**/*.astro"
  - "**/astro.config.*"
---

# Invariants Astro (natif, perf-first)

- **Avant toute feature Astro : invoquer le skill `astro-native`** (vérif API via Context7, zero-JS par défaut, LCP comme garde-fou dur).
